export interface MyDocument {
    documentId: string,
    title: string,
    author: string,
    description?: string,
    numPages: number,
    size?: number,
    type: string,
    format: string,
  }